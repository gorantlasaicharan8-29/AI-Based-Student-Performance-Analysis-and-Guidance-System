[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn -w 4 -b 0.0.0.0:$PORT --chdir backend 'app:create_app()'"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
